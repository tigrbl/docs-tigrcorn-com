services:
  docs-tigrcorn-com:
    build: .
    container_name: docs-tigrcorn-com
    restart: unless-stopped
    labels:
      site.host: docs.tigrcorn.com
      site.kind: zdx-docs
    expose:
      - "80"
