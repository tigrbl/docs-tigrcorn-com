# Getting Started

Use this guide to orient around Tigrcorn documentation.

## Operator commands

```bash
npm ci
npm run check
npm run build
npm run dns:plan
npm run proxy:plan
```

## Deployment model

The docs site builds static output and serves it through a self-hosted Docker service.
